/** Width of the fixed track-label gutter to the left of the browser surface. */
export const TRACK_LABEL_WIDTH = 120;

/** Vertical gap between two stacked tracks. */
export const TRACK_GAP = 8;

export const MARGIN = { top: 10, right: 10, bottom: 10, left: TRACK_LABEL_WIDTH } as const;

/** Height of the reference-coordinate ruler above the tracks. */
export const SCALE_HEIGHT = 24;

/** Vertical space one row of a feature track occupies. */
export const FEATURE_ROW_HEIGHT = 14;

/** Height of the feature boxes drawn within a row. */
export const FEATURE_HEIGHT = 10;

export const DEFAULT_COVERAGE_TRACK_HEIGHT = 50;
