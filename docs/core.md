<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@react-bio-viz/core](./core.md)

## core package

## Functions

<table><thead><tr><th>

Function


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[accentAlpha(alpha)](./core.accentalpha.md)


</td><td>

The accent at a given opacity, e.g. `accentAlpha(0.15)` for a selection wash.


</td></tr>
<tr><td>

[Button({ className, variant, size, asChild, ...props }, input)](./core.button.md)


</td><td>


</td></tr>
<tr><td>

[buttonVariants(props)](./core.buttonvariants.md)


</td><td>


</td></tr>
<tr><td>

[clampToExtent(viewport)](./core.clamptoextent.md)


</td><td>

Clamps a viewport's visible window so it never extends past its own data extent and never collapses to (or past) zero width/height.


</td></tr>
<tr><td>

[computeAxisTicks({ domain, range, minSpacing, includeMinor, }, input)](./core.computeaxisticks.md)


</td><td>

Ticks for a continuous axis (genomic coordinates, query positions) mapped onto a pixel range — the GeneModel/GenomeBrowser/BlastHitDistribution case. Same "nice" step ladder as [computeColumnTicks()](./core.computecolumnticks.md)<!-- -->, so every ruler in the library labels round numbers rather than whatever an even division of the visible span happens to produce.


</td></tr>
<tr><td>

[computeColumnTicks({ columnCount, width, offsetX, pixelsPerColumn, minSpacing, }, input)](./core.computecolumnticks.md)


</td><td>

Ticks for the discrete columns currently visible in a panned/zoomed viewport — the MSA case, where one unit is one column of fixed pixel width. Major ticks carry a 1-based label; minor ticks (five per major where the step allows) do not. Positions are 1-based for the user, so the label `50` sits on column index 49.


</td></tr>
<tr><td>

[configureCanvasForDevicePixelRatio(canvas, cssWidth, cssHeight, devicePixelRatio)](./core.configurecanvasfordevicepixelratio.md)


</td><td>

Sizes a `<canvas>` element's backing store for `devicePixelRatio` (so it isn't blurry on HiDPI screens) while keeping its CSS/layout size at `cssWidth`<!-- -->x`cssHeight`<!-- -->, and returns a 2D context pre-scaled so drawing code can keep working in CSS pixels.


</td></tr>
<tr><td>

[countIntervalRows(intervals)](./core.countintervalrows.md)


</td><td>

How many rows [stackIntervals()](./core.stackintervals.md) used — the minimum lane count needed to avoid overlap.


</td></tr>
<tr><td>

[createCategoricalColorScale(options)](./core.createcategoricalcolorscale.md)


</td><td>

Deterministic, seeded categorical color assignment: the same category string always maps to the same color pair, both within a render and across reloads. Shared by every component that needs to color arbitrary category strings (amino acids, exon/transcript ids, taxon names, BLAST subject ids) — replaces what used to be two independent `randomColor()`<!-- -->+`Color()` call sites (GeneModel and PhyloTree) with subtly different contrast logic.


</td></tr>
<tr><td>

[createControllableStore(initial)](./core.createcontrollablestore.md)


</td><td>

Convenience for the common case: a dedicated Zustand vanilla store for a single controllable value, with no need to hand-roll a `select`<!-- -->/`set` pair. Pass the result straight to [createZustandStoreController()](./core.createzustandstorecontroller.md)<!-- -->, or read/write it directly.


</td></tr>
<tr><td>

[createLinearScale(domain, range)](./core.createlinearscale.md)


</td><td>



</td></tr>
<tr><td>

[createSequentialColorScale(options)](./core.createsequentialcolorscale.md)


</td><td>

A continuous color scale for a numeric metric (e.g. BLAST e-value/bit score/percent identity), as opposed to [createCategoricalColorScale()](./core.createcategoricalcolorscale.md)<!-- -->'s discrete category→color mapping. Linearly interpolates through RGB space between `range`<!-- -->'s two colors; values outside `domain` are clamped to the nearest end rather than extrapolated.


</td></tr>
<tr><td>

[createZustandStoreController(store, select, set)](./core.createzustandstorecontroller.md)


</td><td>

Wraps a Zustand store (or a slice of one) as a [StoreController](./core.storecontroller.md)<!-- -->, the seam `useControllableState` (in `@react-bio-viz/core`<!-- -->) delegates to for the `store` option. This is the reference adapter — the same `StoreController` interface can back Redux, Jotai, an anywidget model (see `@react-bio-viz/python`<!-- -->'s `createAnywidgetStoreController`<!-- -->), or any other external store.


</td></tr>
<tr><td>

[detectSequenceType(sequences)](./core.detectsequencetype.md)


</td><td>

Guesses whether an alignment holds nucleotide or amino-acid sequences, by looking for residues that only occur in the amino-acid alphabet. Used to pick a sensible default color style.


</td></tr>
<tr><td>

[fitToExtent(extent)](./core.fittoextent.md)


</td><td>

A viewport that shows the full extent of `[xMin,xMax] x [yMin,yMax]`<!-- -->, i.e. fully zoomed out.


</td></tr>
<tr><td>

[getDevicePixelRatio()](./core.getdevicepixelratio.md)


</td><td>

`window.devicePixelRatio`<!-- -->, clamped to a minimum of 1 (and 1 outside the browser). Useful on its own when a component needs HiDPI-aware canvas sizing without the rest of [useCoordinateScale()](./core.usecoordinatescale.md)<!-- -->.


</td></tr>
<tr><td>

[moveItem(items, from, to)](./core.moveitem.md)


</td><td>

A copy of `items` with the element at `from` moved to `to`<!-- -->.


</td></tr>
<tr><td>

[panBy(viewport, dx, dy)](./core.panby.md)


</td><td>

Shifts the visible window by `dx`<!-- -->/`dy` data units (positive = pan right/down), clamped to stay within the data extent.


</td></tr>
<tr><td>

[pickNiceLength(target)](./core.picknicelength.md)


</td><td>

Rounds `target` to the nearest "nice" 1/2/5×10ⁿ value — for a legend that should read as a round number near some target size, such as the tree scale bar's branch-length I-beam. Distinct from [pickTickStep()](./core.picktickstep.md)<!-- -->, which picks the smallest nice step at or above a minimum.


</td></tr>
<tr><td>

[pickTickStep(pixelsPerUnit, minSpacing)](./core.picktickstep.md)


</td><td>

Smallest "nice" step that keeps labelled ticks at least `minSpacing` pixels apart at the given zoom level. Steps above the ladder's top are scaled by powers of ten rather than clamped, so this stays sensible on genome-scale axes.


</td></tr>
<tr><td>

[PopoverAnchor({ ...props }, input)](./core.popoveranchor.md)


</td><td>


</td></tr>
<tr><td>

[PopoverBody({ header, renderHeader, renderBody, children }, input)](./core.popoverbody.md)


</td><td>

The popover's floating content. Framework-agnostic replacement for the old Bulma-styled popover: built on Radix + Tailwind (compiled away at build time, see the `bio-viz-conventions` skill) instead of `react-popper` + Bulma classes, and with `renderHeader`<!-- -->/`renderBody` override slots so GeneModel/GenomeBrowser/BlastHitDistribution can each customize presentation without a new implementation.


</td></tr>
<tr><td>

[PopoverContent({ className, align, sideOffset, ...props }, input)](./core.popovercontent.md)


</td><td>


</td></tr>
<tr><td>

[PopoverDescription({ className, ...props }, input)](./core.popoverdescription.md)


</td><td>


</td></tr>
<tr><td>

[PopoverHeader({ className, ...props }, input)](./core.popoverheader.md)


</td><td>


</td></tr>
<tr><td>

[PopoverTitle({ className, ...props }, input)](./core.popovertitle.md)


</td><td>


</td></tr>
<tr><td>

[qualityGradient(score, darkMode)](./core.qualitygradient.md)


</td><td>

A blue→red gradient over a 0–1 score, for any per-column or per-residue quality/conservation metric. Desaturated and darkened in dark mode so it doesn't glare against a dark background.


</td></tr>
<tr><td>

[residueColor(char, style, darkMode)](./core.residuecolor.md)


</td><td>

Looks up the color a residue character gets under `style`<!-- -->. Unknown characters fall back to the gap color for a scheme that recognizes the alphabet, so an unexpected residue reads as "nothing here" rather than as a real category.


</td></tr>
<tr><td>

[ResizeHandle({ orientation, size, onResize, min, max, thickness, className, "aria-label": ariaLabel, }, input)](./core.resizehandle.md)


</td><td>

A draggable divider between two panels (an MSA's label column and its canvas, a track and the alignment above it). Deliberately stateless: it reports sizes and the owning component decides where they live — which, per the controllable-state convention, is usually a `panelSizes` prop.


</td></tr>
<tr><td>

[resolveRowOrder(rowIds, order)](./core.resolveroworder.md)


</td><td>

The display order as indices into `msa`<!-- -->: `order`<!-- -->'s ids first (unknown ids ignored), then any rows `order` doesn't mention, in their original order. So a stale or partial order — e.g. a tree's leaf order that lacks some sequences — degrades gracefully instead of hiding rows.


</td></tr>
<tr><td>

[RowLabels({ rows, rowHeight, offsetY, width, height, fontSize, textAlign, hoverIndex, onHoverIndexChange, selectedIds, onRowClick, onRename, onRemove, onReorder, onReorderPreview, className, }, input)](./core.rowlabels.md)


</td><td>

A virtualised column of row labels that stays aligned with a panned/zoomed row axis — the MSA's sequence names, the distance matrix's row names. Hover, click-to-select, inline rename, remove, and drag-to-reorder (with a live preview reported through `onReorderPreview`<!-- -->) are each enabled by passing the matching callback, so a read-only column is just `rows`<!-- -->/`rowHeight`<!-- -->/`offsetY`<!-- -->.


</td></tr>
<tr><td>

[Select({ ...props }, input)](./core.select.md)


</td><td>


</td></tr>
<tr><td>

[SelectContent({ className, children, position, align, ...props }, input)](./core.selectcontent.md)


</td><td>


</td></tr>
<tr><td>

[SelectGroup({ className, ...props }, input)](./core.selectgroup.md)


</td><td>


</td></tr>
<tr><td>

[SelectItem({ className, children, ...props }, input)](./core.selectitem.md)


</td><td>


</td></tr>
<tr><td>

[SelectLabel({ className, ...props }, input)](./core.selectlabel.md)


</td><td>


</td></tr>
<tr><td>

[SelectScrollDownButton({ className, ...props }, input)](./core.selectscrolldownbutton.md)


</td><td>


</td></tr>
<tr><td>

[SelectScrollUpButton({ className, ...props }, input)](./core.selectscrollupbutton.md)


</td><td>


</td></tr>
<tr><td>

[SelectSeparator({ className, ...props }, input)](./core.selectseparator.md)


</td><td>


</td></tr>
<tr><td>

[SelectTrigger({ className, size, children, ...props }, input)](./core.selecttrigger.md)


</td><td>


</td></tr>
<tr><td>

[SelectValue({ ...props }, input)](./core.selectvalue.md)


</td><td>


</td></tr>
<tr><td>

[serializeSvg(svg, options)](./core.serializesvg.md)


</td><td>

Serializes a rendered `<svg>` to a standalone SVG document. The library styles SVG partly with class-based CSS and theme variables (`currentColor`<!-- -->, `var(--background)`<!-- -->), none of which travel with the markup — so the computed style of every element is inlined first, and the result looks the same outside the page. Interactive-only elements (invisible hit areas) are dropped.


</td></tr>
<tr><td>

[Slider({ className, defaultValue, value, min, max, ...props }, input)](./core.slider.md)


</td><td>


</td></tr>
<tr><td>

[stackIntervals(intervals)](./core.stackintervals.md)


</td><td>

Greedily assigns each interval a row index (0-based) such that no two intervals sharing a row overlap — an interval is placed in the first row whose last-placed interval ends before this one starts, else a new row is opened. Sorts by start position first, so row assignment is deterministic regardless of input order. Shared by any track/lane-style renderer that needs to avoid overlapping intervals (GenomeBrowser's feature track, BlastHitDistribution's hit rows).


</td></tr>
<tr><td>

[svgToPng(svgText, scale)](./core.svgtopng.md)


</td><td>

Rasterises an SVG document (e.g. from [serializeSvg()](./core.serializesvg.md)<!-- -->) to a PNG blob at `scale` × its size.


</td></tr>
<tr><td>

[Toggle({ className, variant, size, ...props }, input)](./core.toggle.md)


</td><td>


</td></tr>
<tr><td>

[ToggleGroup({ className, variant, size, spacing, orientation, children, ...props }, input)](./core.togglegroup.md)


</td><td>


</td></tr>
<tr><td>

[ToggleGroupItem({ className, children, variant, size, ...props }, input)](./core.togglegroupitem.md)


</td><td>


</td></tr>
<tr><td>

[toggleVariants(props)](./core.togglevariants.md)


</td><td>


</td></tr>
<tr><td>

[Tooltip({ ...props }, input)](./core.tooltip.md)


</td><td>


</td></tr>
<tr><td>

[TooltipContent({ className, sideOffset, children, ...props }, input)](./core.tooltipcontent.md)


</td><td>


</td></tr>
<tr><td>

[TooltipProvider({ delayDuration, ...props }, input)](./core.tooltipprovider.md)


</td><td>


</td></tr>
<tr><td>

[TooltipTrigger({ ...props }, input)](./core.tooltiptrigger.md)


</td><td>


</td></tr>
<tr><td>

[toRGBA(color)](./core.torgba.md)


</td><td>

Resolves any CSS colour string the library's colour functions produce (`#rrggbb`<!-- -->, `rgb()`<!-- -->, `hsl()`<!-- -->, named colours like `royalblue`<!-- -->) to 8-bit RGBA, memoised per string. Pixel-level canvas rendering (the MSA's zoomed-out view and minimap, the distance heatmap) writes `ImageData` directly, which needs numbers rather than a `fillStyle` string. An unparseable string resolves to opaque black rather than throwing, so one bad colour can't blank a whole render.


</td></tr>
<tr><td>

[useControllableState(options)](./core.usecontrollablestate.md)


</td><td>

The single state primitive every stateful prop in this library is built on. Supports three modes, chosen by which options are passed, all producing the same `[value, setValue]` shape:

- `value` present, no `store`<!-- -->: fully controlled — the consumer owns the value; `setValue` only calls `onChange`<!-- -->, it never updates anything locally. - `value` absent, no `store`<!-- -->: uncontrolled — backed by local React state seeded from `defaultValue`<!-- -->; `onChange` still fires on every update so an observing-only parent works too. - `store` present: delegates to the external [StoreController](./core.storecontroller.md) via `useSyncExternalStore` instead of local state. Takes priority over `value`<!-- -->/`defaultValue` when present.

See the `bio-viz-conventions` project skill for the full contract and the convention every component follows for wrapping this under a domain-specific prop name (e.g. `viewport`<!-- -->/ `defaultViewport`<!-- -->/`onViewportChange`<!-- -->/`viewportStore`<!-- -->).


</td></tr>
<tr><td>

[useCoordinateScale(options)](./core.usecoordinatescale.md)


</td><td>

One shared way to turn a [Viewport](./core.viewport.md) into pixel-space scales, so SVG renderers (GeneModel, PhyloTree) and Canvas renderers (MultipleSequenceAlignment) derive their coordinates from the same source instead of each hand-rolling `d3.scaleLinear().domain().range()` slightly differently.


</td></tr>
<tr><td>

[useDarkMode()](./core.usedarkmode.md)


</td><td>

Whether the host app is currently in dark mode, following the same convention the library's stylesheet uses — a `dark` class on `<html>` — and re-rendering when that class is toggled.

SVG surfaces should prefer `currentColor` and theme tokens (`text-muted-foreground`<!-- -->, `border`<!-- -->) over this hook: they follow the theme for free. It exists for canvas rendering, which has no access to CSS custom properties and so needs a concrete light/dark decision — see [residueColor()](./core.residuecolor.md)<!-- -->'s `darkMode` argument.


</td></tr>
<tr><td>

[useDragPan({ onPan, scaleX, scaleY, disabled }, input)](./core.usedragpan.md)


</td><td>

Pointer-event drag-to-pan, generic over any element with a pixel-to-data-unit scale. Spread the returned handlers onto the interactive surface (an `<svg>`<!-- -->, a `<canvas>`<!-- -->, a wrapper `<div>`<!-- -->).

If the surface contains nested elements with their own click behavior (a feature marker with a popover, say), mark them with `data-pan-ignore` so a click there opens the popover instead of being captured as the start of a drag.


</td></tr>
<tr><td>

[useViewport(options)](./core.useviewport.md)


</td><td>

The shared pan/zoom primitive for every component with a "large virtual coordinate space, visible window" surface (MultipleSequenceAlignment, GeneModel, GenomeBrowser, and BlastHitDistribution's query axis). Composes [useControllableState()](./core.usecontrollablestate.md) with the pure [Viewport](./core.viewport.md) operations, so it supports controlled/uncontrolled/store-backed usage exactly like every other stateful prop in this library — see the `bio-viz-conventions` project skill.


</td></tr>
<tr><td>

[useWheelZoom({ onZoom, toDataPoint, onPan, sensitivity, disabled, }, input)](./core.usewheelzoom.md)


</td><td>

Wheel/trackpad-pinch-to-zoom, zooming around the pointer's position rather than the viewport center — or, with `onPan`<!-- -->, scroll-to-pan plus Ctrl/⌘-scroll-to-zoom. Spread the returned object onto the interactive surface; it attaches a native, non-passive listener so scrolling the surface never also scrolls the page.


</td></tr>
<tr><td>

[ViewportToolbar({ viewport, panBy, zoomBy, reset, axes }, input)](./core.viewporttoolbar.md)


</td><td>

Shared pan/zoom toolbar for any [useViewport()](./core.useviewport.md)<!-- -->-backed component (MultipleSequenceAlignment, GeneModel, PhyloTree, GenomeBrowser), built on shadcn/ui's `Button`<!-- -->. `axes` controls which pan directions are shown — a 1D viewport (e.g. GeneModel's genomic coordinate) only needs `"x"`<!-- -->.


</td></tr>
<tr><td>

[zoomAt(viewport, point, factor, factorY)](./core.zoomat.md)


</td><td>

Scales the visible window by `factor` around a fixed data-coordinate point (so that point stays under the cursor), clamped to the data extent. `factor < 1` zooms in, `factor > 1` zooms out. Pass `factorY` to zoom the axes independently (`1` keeps that axis fixed) — e.g. the MSA's column-only zoom, which keeps rows readable.


</td></tr>
<tr><td>

[zoomBy(viewport, factor)](./core.zoomby.md)


</td><td>

Scales the visible window by `factor` (`<1` zooms in, `>1` zooms out) around its own center.


</td></tr>
</tbody></table>

## Interfaces

<table><thead><tr><th>

Interface


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[CategoricalColor](./core.categoricalcolor.md)


</td><td>

A base/contrast color pair for one category, derived deterministically from its name.


</td></tr>
<tr><td>

[CategoricalColorScaleOptions](./core.categoricalcolorscaleoptions.md)


</td><td>



</td></tr>
<tr><td>

[ColorScheme](./core.colorscheme.md)


</td><td>

A residue-coloring scheme in both light and dark variants.


</td></tr>
<tr><td>

[ControllableStateOptions](./core.controllablestateoptions.md)


</td><td>

Options accepted by `useControllableState` (in `@react-bio-viz/core`<!-- -->). Every stateful prop in this library is a thin, domain-named wrapper around this shape — see the `bio-viz-conventions` project skill.


</td></tr>
<tr><td>

[CoordinateScale](./core.coordinatescale.md)


</td><td>



</td></tr>
<tr><td>

[IntervalLike](./core.intervallike.md)


</td><td>

The minimum shape [stackIntervals()](./core.stackintervals.md) needs from an interval-like item.


</td></tr>
<tr><td>

[LinearScale](./core.linearscale.md)


</td><td>

A minimal linear scale: maps a numeric domain to a numeric range (and back), with the small subset of the d3-scale `scaleLinear` API this library actually uses (`domain`<!-- -->/`range`<!-- -->/ `invert`<!-- -->). Hand-rolled to avoid depending on d3 for what is, here, always a single linear interpolation — see the `bio-viz-conventions` project skill.


</td></tr>
<tr><td>

[PopoverBodyProps](./core.popoverbodyprops.md)


</td><td>



</td></tr>
<tr><td>

[ResizeHandleProps](./core.resizehandleprops.md)


</td><td>



</td></tr>
<tr><td>

[RowLabel](./core.rowlabel.md)


</td><td>

One row of a [RowLabels()](./core.rowlabels.md) column.


</td></tr>
<tr><td>

[RowLabelsProps](./core.rowlabelsprops.md)


</td><td>



</td></tr>
<tr><td>

[RowReorderPreview](./core.rowreorderpreview.md)


</td><td>

An in-progress drag: row `from` (a display index) is currently hovering over index `to`<!-- -->.


</td></tr>
<tr><td>

[SequentialColorScaleOptions](./core.sequentialcolorscaleoptions.md)


</td><td>



</td></tr>
<tr><td>

[SerializeSvgOptions](./core.serializesvgoptions.md)


</td><td>



</td></tr>
<tr><td>

[StoreController](./core.storecontroller.md)


</td><td>

A minimal external-store seam that `useControllableState` (in `@react-bio-viz/core`<!-- -->) can delegate to instead of plain React state. Any store — Zustand, an anywidget model, a hand-rolled event emitter — can satisfy this by implementing `getValue`<!-- -->/`setValue`<!-- -->/`subscribe`<!-- -->.


</td></tr>
<tr><td>

[Tick](./core.tick.md)


</td><td>

One tick on an axis.


</td></tr>
<tr><td>

[UseCoordinateScaleOptions](./core.usecoordinatescaleoptions.md)


</td><td>



</td></tr>
<tr><td>

[UseDragPanHandlers](./core.usedragpanhandlers.md)


</td><td>



</td></tr>
<tr><td>

[UseDragPanOptions](./core.usedragpanoptions.md)


</td><td>



</td></tr>
<tr><td>

[UseViewportOptions](./core.useviewportoptions.md)


</td><td>



</td></tr>
<tr><td>

[UseViewportResult](./core.useviewportresult.md)


</td><td>



</td></tr>
<tr><td>

[UseWheelZoomHandlers](./core.usewheelzoomhandlers.md)


</td><td>



</td></tr>
<tr><td>

[UseWheelZoomOptions](./core.usewheelzoomoptions.md)


</td><td>



</td></tr>
<tr><td>

[Viewport](./core.viewport.md)


</td><td>

A plain, serializable "virtual coordinate space + visible window" shape shared by every component that needs pan/zoom (MSA, GeneModel, GenomeBrowser, BlastHitDistribution's axis). Deliberately plain data, not a class with mutation methods, so it survives living in a Zustand store, being sent over an anywidget/Jupyter comm channel as JSON, or being diffed by React.


</td></tr>
<tr><td>

[ViewportToolbarProps](./core.viewporttoolbarprops.md)


</td><td>



</td></tr>
<tr><td>

[ZustandLikeStore](./core.zustandlikestore.md)


</td><td>

The part of a Zustand store's API that [createZustandStoreController()](./core.createzustandstorecontroller.md) uses. Declared structurally rather than imported from `zustand`<!-- -->, so this library's published types resolve without `zustand` installed — any real Zustand store (vanilla or the `create()` hook, which exposes the same methods) satisfies it.


</td></tr>
</tbody></table>

## Variables

<table><thead><tr><th>

Variable


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[aaClustalXScheme](./core.aaclustalxscheme.md)


</td><td>

Amino-acid scheme grouping residues by ClustalX/Jalview's physicochemical classes.


</td></tr>
<tr><td>

[aaTaylorScheme](./core.aataylorscheme.md)


</td><td>

Amino-acid scheme giving every residue its own spectral color (Taylor).


</td></tr>
<tr><td>

[aaZappoScheme](./core.aazapposcheme.md)


</td><td>

Amino-acid scheme grouping residues by Zappo physicochemical property.


</td></tr>
<tr><td>

[ACCENT\_COLOR](./core.accent_color.md)


</td><td>

The CSS custom property holding the visualization accent — the one saturated colour the library reserves for "you can interact with this" (hover outlines, selection strokes, the MSA cursor cross-hair). Defined for both themes in the shipped stylesheet, so SVG can use it directly and follow light/dark for free.


</td></tr>
<tr><td>

[ACCENT\_LITERAL](./core.accent_literal.md)


</td><td>

Literal accent values, for canvas rendering — a `<canvas>` context can't resolve a CSS custom property, so it needs the concrete colour for the current theme. SVG should use [ACCENT\_COLOR](./core.accent_color.md) instead and let CSS pick.


</td></tr>
<tr><td>

[ACCENT\_RGB](./core.accent_rgb.md)


</td><td>

The same accent as a bare `r, g, b` triple, for `rgb(... / alpha)` composition.


</td></tr>
<tr><td>

[AMINO\_ACIDS](./core.amino_acids.md)


</td><td>



</td></tr>
<tr><td>

[DEFAULT\_COLOR\_STYLE](./core.default_color_style.md)


</td><td>

The style each sequence type defaults to when none is chosen explicitly.


</td></tr>
<tr><td>

[dnaClustalXScheme](./core.dnaclustalxscheme.md)


</td><td>

Nucleotide scheme using ClustalX's traditional palette.


</td></tr>
<tr><td>

[dnaScheme](./core.dnascheme.md)


</td><td>

Default nucleotide scheme: A/C/G/T in green/blue/orange/red.


</td></tr>
<tr><td>

[GAP\_COLOR](./core.gap_color.md)


</td><td>

Neutral fill for gap characters (`-`<!-- -->). Light/dark pair, because canvas rendering can't fall back to a CSS theme token the way SVG can — see [residueColor()](./core.residuecolor.md)<!-- -->'s `darkMode` argument.


</td></tr>
<tr><td>

[MIN\_VIEWPORT\_SPAN](./core.min_viewport_span.md)


</td><td>

Minimum visible span (in either axis) that zooming in is allowed to reach.


</td></tr>
<tr><td>

[NUCLEOTIDES](./core.nucleotides.md)


</td><td>



</td></tr>
<tr><td>

[Popover](./core.popover.md)


</td><td>

Opens/closes the popover; wraps shadcn/ui's Radix-based `Popover` root.


</td></tr>
<tr><td>

[PopoverTrigger](./core.popovertrigger.md)


</td><td>

Wrap the element that should open the popover on click.


</td></tr>
<tr><td>

[RESIDUE\_COLOR\_STYLES](./core.residue_color_styles.md)


</td><td>

The color styles that map a single residue character to a color.


</td></tr>
<tr><td>

[ROOT\_CLASS](./core.root_class.md)


</td><td>

Class every component's root element (and every portalled overlay) carries. The shipped stylesheet scopes its small element reset to this class instead of resetting the whole page, since it is loaded into a host application it must not restyle.


</td></tr>
<tr><td>

[SELECTION\_LITERAL](./core.selection_literal.md)


</td><td>

Selected rows/columns/cells. A warm red rather than the accent, so a selection stays distinct from hover feedback drawn on top of it. Literal (not a CSS variable) because the MSA paints it on canvas; it reads on both light and dark backgrounds.


</td></tr>
<tr><td>

[UNKNOWN\_COLOR](./core.unknown_color.md)


</td><td>

Fill for a character no scheme has an entry for.


</td></tr>
</tbody></table>

## Type Aliases

<table><thead><tr><th>

Type Alias


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[AminoAcid](./core.aminoacid.md)


</td><td>



</td></tr>
<tr><td>

[BioLetter](./core.bioletter.md)


</td><td>



</td></tr>
<tr><td>

[CategoricalColorScale](./core.categoricalcolorscale.md)


</td><td>



</td></tr>
<tr><td>

[ControllableStateChangeSource](./core.controllablestatechangesource.md)


</td><td>



</td></tr>
<tr><td>

[Nucleotide](./core.nucleotide.md)


</td><td>



</td></tr>
<tr><td>

[Palette](./core.palette.md)


</td><td>

A named category → color lookup, generalized beyond any single component's domain.


</td></tr>
<tr><td>

[ResidueColorStyle](./core.residuecolorstyle.md)


</td><td>



</td></tr>
<tr><td>

[RGBA](./core.rgba.md)


</td><td>

An 8-bit `[r, g, b, a]` quadruple, ready to write into `ImageData`<!-- -->.


</td></tr>
<tr><td>

[SequenceType](./core.sequencetype.md)


</td><td>

Whether a sequence reads as nucleotide or amino-acid data.


</td></tr>
<tr><td>

[SequentialColorScale](./core.sequentialcolorscale.md)


</td><td>



</td></tr>
</tbody></table>

